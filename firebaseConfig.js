// Firebase setup temporarily disabled to prevent Render Error
// import { initializeApp } from "firebase/app";
// import { getAuth } from "firebase/auth";

const auth = {}; // Dummy auth object

export { auth };
