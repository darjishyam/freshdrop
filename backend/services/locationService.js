const fetchRestaurants = async (lat, lon, radius = 5000) => {
  return [];
};

const fetchGroceries = async (query = "snacks") => {
  return [];
};

module.exports = {
  fetchRestaurants,
  fetchGroceries,
};
